const sqlcreds = {
    connectionLimit : 10,
    host            : 'localhost',
    user            : 'root',
    password        : 'root',
    database        : 'db_rokuusers',
    port            : 8889 //8889
}

module.exports = sqlcreds;